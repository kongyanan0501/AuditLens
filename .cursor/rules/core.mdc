---
description: AuditLens 全局编码标准与模块边界
alwaysApply: true
---

# AuditLens Core

## 原则

- 最小 diff：只改任务相关文件，不做无关重构
- TypeScript strict；领域类型放 `types/audit.ts`
- 代码/标识符英文；面向用户的 UI 文案中文

## 依赖方向（强制）

```text
app/ → server/ → lib/
app/ → components/
types/ ← 所有层（types 不 import 业务代码）
```

禁止：`server/` import `components/`；`lib/` import `server/`。

## 修改前

1. 查 `todo.md` 当前 Phase
2. 查 `PROJECT.md` 定位模块
3. 涉及数据库：先读 `supabase/schema.md`
4. 涉及业务规则/阈值/评分/RAG：先读 `docs/business-decisions.md`
5. 完成后跑 `npm run lint` 和 `npm run build`

## 业务决策同步（强制）

**[`docs/business-decisions.md`](docs/business-decisions.md)** 是业务规则、阈值、评分、RAG/LLM 行为、API 业务约束的 canonical 文档。

每次变更业务决策（规则阈值、评分公式、解析语义、LLM 范围、持久化字段含义等），**同一提交**内必须：

1. 更新 **`docs/business-decisions.md`** 对应章节
2. 在文档 **变更日志** 追加一行

详见 `.cursor/rules/business-decisions.mdc`。

## 数据库 Schema 同步（强制）

**`supabase/schema.md`** 是表结构的 canonical 文档。

每次变更数据库（migration / SQL Editor / Dashboard 改表）后，**同一提交**内必须：

1. 更新 `supabase/migrations/*.sql`
2. 更新 **`supabase/schema.md`**（表、RLS、索引、变更日志）
3. 同步 `types/database.ts`（及必要时 `types/audit.ts`）

详见 `.cursor/rules/database.mdc`。

## 安全

- 禁止硬编码 API Key
- 禁止 client 组件使用 `OPENAI_API_KEY` / `PINECONE_API_KEY`
- `audit_*` 查询必须按 `user_id` 过滤

## 可插拔

LLM 与向量库只通过 `lib/ai-provider.ts`、`lib/pinecone.ts` 工厂获取，不在业务节点内直接实例化 SDK。
